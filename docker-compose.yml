version: "3"
services:
  lambda:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - ./lambda_function:/app/lambda_function
